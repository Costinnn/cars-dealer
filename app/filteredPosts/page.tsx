import React from "react";
import { FilteredPostsTypes } from "@/types/globalTypes";

import "./style.scss";

const page = ({ searchParams }: { searchParams: FilteredPostsTypes }) => {
  // console.log(searchParams);
  return <main className="page-filteredPosts">TYPE</main>;
};

export default page;
